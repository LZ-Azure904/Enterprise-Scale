on: [push]
name: Azure ARM for Bpxtest azure2.0 prototype
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:

      # Checkout code
    - uses: actions/checkout@main

      # Log into Azure
    - uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

      # Deploy ARM template
    - name: Run ARM deploy
      uses: azure/arm-deploy@v1
      with:
        scope: managementgroup
        managementGroupId: e8ffc671-bfab-4391-8de4-ccc8d28f8f11
        region: ${{ secrets.AZURE_REGION }}
        template: ./docs/reference/contoso/armTemplates/auxiliary/bpxtest-mgmtGroups.json
        deploymentName: bpx azure2.0 mgmt structure mvp

      # output containerName variable from template
    - run: echo ${{ steps.deploy.outputs.containerName }}